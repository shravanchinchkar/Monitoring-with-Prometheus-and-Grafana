global:
  scrape_interval: 4s # after every 4 seconds the prometheus server calls the /metrics endpoint

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["192.168.0.102:8000"]